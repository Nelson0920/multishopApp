import React from "react";

const AuxiliarRules = ({ rules }) => {
      return (
            <div className="auxiliar-rules">
                  {rules}
            </div>
      );
};

export default AuxiliarRules;
